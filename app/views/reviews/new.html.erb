<div class="container">
  <h1>Create a new review</h1>

  <%= simple_form_for [@pub, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.button :submit %>
  <% end %>
</div>
